package http

const (
	// DefaultHTTPUrl is the default URL to send HTTP requests to.
	DefaultHTTPUrl = "http://localhost"
	// DefaultMethod is the default HTTP method to use. Other methods
	// are not supported at this time, but as I (or someone else) needs
	// them they will be added.
	DefaultMethod = "POST"
)
